#!/usr/bin/env php
<?php
namespace Readership\Map;

require_once __DIR__ . '/../src/autoload.php';
ini_set('log_errors_max_len', 0);

$validator = new DataValidator();
exit($validator->validFile($argv[1]));
